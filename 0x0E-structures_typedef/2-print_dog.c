#include "dog.h"
#include <stdlio.h>

/**
 * print_dog - prints a dog
 * @d : contains the dog's information
 * Return : void
 */
(void print_dog(struct dog *d)
{
	printf("Name: %s\n", d->name);
	if (name == NULL)
	{
	printf("Name : (nil)");
	}
	printf("Age: %f\n", d->age);
	if (age == NULL)
	{
	printf("Age:(nil)");
	}
	printf("Owner:%s\n", d->owner);
	if (owner == NULL)
	{
	printf("Owner: (nill)")
	}




}
