#include <stdio.h>
/**
 * main  - Entry point
 * Description: a C program prints using puts 
 * Return : always returns 0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
